# docs/plan/archive

这里放已经退出 live planning root 的历史计划。

- `docs/plan/` 根目录只保留当前 canonical 计划。
- 这里的文档仍有审计或背景价值，但不再是当前执行入口。
- 如果某个 live 文档需要引用这里的内容，优先同时给出对应的 `reports/` closeout 或 `docs/experiments/registry.md` 入口。

当前分组：

- `foundation/`
- `genos/`
- `scheduling/`
- `v2/`
- `compare/`
- `governance/`
