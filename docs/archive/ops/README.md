# docs/archive/ops

这里保留已经退出 `docs/research/` 的执行型 runbook、下载记录和操作日志。

- `docs/research/` 只保留稳定研究摘要。
- 这里的文档可以解释历史操作细节，但不应再被当成 live 实验状态来源。
- 当前 live 状态统一看 `TRACKING.md`、`docs/experiments/registry.md` 和 `docs/experiments/runs.csv`。
