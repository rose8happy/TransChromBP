# post-20260406 delta

## 这份补丁文件是什么

这不是一套新外发包，而是对 `2026-04-05` 那套主包的后续增量更新。

它的作用有四个：

1. 把 `2026-04-06` 之后真正新增的高信息量事实补进去；
2. 明确哪些旧开放问题现在已经被收口，不需要再重复裁决；
3. 把 6000 的实际运行仓 / 环境说明补清楚，避免误用路径；
4. 为“下一代不同方向该怎么选”提供更明确的边界条件。

---

## 1. 这之后真正新增了什么

`2026-04-05` 外发之后，foundation 线又完成了一个当时唯一仍被允许的 genuinely new-hypothesis 重入：

- `NT v2`
- `layer_07__bins16_mean`
- `center-aligned / token-aware residual`
- `short10`
- 双卡训练 + full held-out `test-full`

对应 run：

- `ntv2_bins16_centerres_short10_s42_20260406_dual`

对应 held-out JSON：

- `/data1/zhoujiazhen/bylw_atac/TransChromBP/outputs/metrics/ntv2_bins16_centerres_short10_s42_20260406_dual_best_test_full_20260406_032529.json`

---

## 2. 新结果本身

相对 matched baseline

- `short10_nofoundation_control_s42_20260405_dual_best_test_full_20260405_2100.json`

`bins16 center-aligned residual` 仍明显更差：

### overall

- baseline:
  - `count_pearson_full=0.8457`
  - `profile_target_jsd_full_mean=0.4395`
  - `profile_pearson_full_mean=0.7116`
- bins16:
  - `count_pearson_full=0.7727`
  - `profile_target_jsd_full_mean=0.4625`
  - `profile_pearson_full_mean=0.6580`

### peak

- baseline:
  - `count_pearson_full=0.8298`
  - `profile_target_jsd_full_mean=0.3193`
  - `profile_pearson_full_mean=0.8641`
- bins16:
  - `count_pearson_full=0.7516`
  - `profile_target_jsd_full_mean=0.3588`
  - `profile_pearson_full_mean=0.7837`

一句话说：

> 即便把旧 `bins4 + coarse summary residual` 升级成真正更像“中心对齐 token residual”的 `bins16` 版本，结果也没有回到 matched no-foundation baseline 之上。

---

## 3. 这条新增结果真正说明了什么

它强化了下面这件事：

> 当前已经测过的 `NT v2 residual short10 family`
> `summary / token-fusion / coarse residual / bins16 center-aligned residual`
> 默认停表。

它**没有**说明：

- 所有可能的 foundation model 路线都被数学上否证；
- 所有 token/base-level 路线都永远无效；
- 纯因为工程没调通才失败。

因此，这条新增事实的用途应该是：

> 给“下一代不同方向该怎么选”提供更清楚的边界条件，
> 而不是继续邀请大家沿当前 measured family 微调。

---

## 4. 因此哪些旧问题现在已经收口

这轮外部模型**不需要**再重复裁决下面这些问题：

- “foundation model 现在要不要升成新的项目主线？”
- “Genos / Caduceus / NT v2 里谁更值得立刻继续扩当前 measured family？”
- “是不是应该继续追 residual short10 family 的下一个自然变体？”

截至当前时点，这些问题的默认答案已经收口为：

- 不升主线；
- 不自动扩线；
- 不继续把当前 measured family 当作自然后续链。

---

## 5. 当前更值得外部模型看的技术问题

当前更值得外部模型做的是：

- 在当前 stop-rule 下，最有潜力的下一代提升路线是什么；
- AlphaGenome / U-Net 风格是否值得借鉴；
- ATAC 任务中应补哪些指标，才能更好回答评审和答辩会问的问题；
- 预训练基因组大模型在官方或常见下游做法中通常怎样接到 ATAC/可及性任务里；
- 为什么我们当前接入没有形成效果，以及接下来该怎样改；
- 我们应怎样与 AlphaGenome 做公平且有解释力的对比。

请不要把主要注意力重新放回旧的 `Transformer/bias` 连接怀疑上；那现在只应被视为历史背景，而不是当前主问题。

---

## 6. 6000 runtime workspace / env note

这点请显式默认知道：

- 本地归档仓主要把 TransChromBP 快照放在 `vendor/transchrombp/...`
- 6000 实际运行仓是 `/data1/zhoujiazhen/bylw_atac/TransChromBP`
- 该运行仓使用的是 `src/transchrombp/...` 布局
- foundation 训练 / 评估 / 代码自检应优先使用：
  - `/data1/zhoujiazhen/bylw_atac/.venvs/genos-1.2b/bin/python`
- `chrombpnet` 环境适合官方 ChromBPNet / `modisco` / `bedGraphToBigWig` 等工具，但不适合直接拿来做 foundation runtime 核验

这条说明只是为了避免路径和环境误解，不影响上面那些结果本身。

---

## 7. 现在怎么读这套包

请把当前外发材料理解成：

- `00-05`：`2026-04-05` 时点的主包
- `08`：`2026-04-06` 之后的关键增量边界
- `09`：本轮真正想问外部模型的技术路线问题

因此，当前外部模型若要给建议，应该基于：

> “主线基座已经比较稳定，当前 measured foundation family 已经被收口，问题不再是旧误会是否成立，而是下一代提升方向、指标和 AlphaGenome 对比该怎么选。”
