# references

该目录用于收纳本地查阅用的参考材料，例如论文 PDF、安装包、临时下载的补丁包。

规则：

- Git 只保留轻量级说明文件和索引。
- 大文件本体默认放在 `references/local-only/`，继续保持本地自留，不进入版本库。
- 如果某份材料已经被整理成正式学习笔记、报告摘要或可复用资产，应迁入 `docs/learning/`、`reports/` 或其它正式目录，而不是继续堆在这里。
